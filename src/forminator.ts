/**
 * @public
 */
export interface Options {}

/**
 *
 * return `forminator` string
 *
 * @returns the `forminator`  string
 *
 * @example
 * Here's an example:
 *
 * ```ts
 * import { Forminator } from '@forminator/forminator'
 *
 * console.log(Forminator());
 * // Prints "forminator":
 * ```
 */
export function Forminator(options?: Options) {
  return 'forminator';
}
