import { forminatorOption } from './forminator-option';
describe('forminatorOption', () => {
  it('return forminator-option', () => {
    expect(forminatorOption()).toBe('forminator-option');
  });
});
